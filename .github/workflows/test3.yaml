name: File operations in github-push

on: push

jobs:
    files:
        runs-on: ubuntu-latest
        steps:
            - name: create a one file
              run: echo "KK DEVOPS 123456" > devops.txt

            - name: display the content of a file
              run: cat devops.txt

            - name: display the date
              run: date
            
            - name: display files and dir 
              run: ls -lrth

            - name: dispaly the present location
              run: pwd


            

